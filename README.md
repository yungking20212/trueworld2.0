# trueworld2.0 🛡️🌍🚀💎

Witness the neural future of social reality. **Trueworld 2.0** is an immersive planetary visual network powered by high-precision geospatial data and atomic social pulses.

## ⚖️ Community Guidelines (Neural Protocol)

To maintain the integrity of the planetary network, all users must adhere to the following protocols:

1.  **Authentic Broadcasts**: Only the most genuine visual experiences should permeate the network.
2.  **Respect the Territory**: Engaging in "Own Your Block" competitions should be done with digital honor.
3.  **Neural Safety**: No content that violates the collective well-being of the Trueworld ecosystem.
4.  **Vantage Integrity**: Exploiting the neural score or XP accumulator through artificial means is prohibited.

## ⚙️ Settings & Privacy

The **Strategic POV** dashboard allows users to manage their neural presence:
- **Privacy Guard**: Activate high-precision location masking for sensitive broadcasts.
- **Neural Longevity**: Define story expiration protocols (24h Standard / 48h Elite).
- **Monetization Engine**: Link to the planetary banking system (Stripe Connect) to activate real-time revenue streams.

## ⚡ Real Backend Implementation (Social Foundations)

The following SQL RPCs are required to activate the visual social engines in the Supabase planetary database:

```sql
-- 1. Video Like Increment
CREATE OR REPLACE FUNCTION increment_like_count(v_id uuid)
RETURNS void AS $$
BEGIN
  UPDATE videos SET likes = likes + 1 WHERE id = v_id;
END;
$$ LANGUAGE plpgsql;

-- 2. Video Like Decrement
CREATE OR REPLACE FUNCTION decrement_like_count(v_id uuid)
RETURNS void AS $$
BEGIN
  UPDATE videos SET likes = GREATEST(0, likes - 1) WHERE id = v_id;
END;
$$ LANGUAGE plpgsql;

-- 3. Comment Like Increment
CREATE OR REPLACE FUNCTION increment_comment_likes(c_id uuid)
RETURNS void AS $$
BEGIN
  UPDATE video_comments SET likes = likes + 1 WHERE id = c_id;
END;
$$ LANGUAGE plpgsql;

-- 4. Video View Counter + XP Accumulator
CREATE OR REPLACE FUNCTION register_video_view(v_id uuid)
RETURNS void AS $$
BEGIN
  UPDATE videos SET views_count = views_count + 1 WHERE id = v_id;
END;
$$ LANGUAGE plpgsql;
```

---
*Generated by Antigravity - The Future of Agentic Coding.*
